{
  "name": "<%= extName.toLowerCase() %>",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "sfdx",
    "sfdxtend",
    "extend",
    "extension",
    "yeoman-generator"
  ],
  "dependencies": {
    "yeoman-generator": "^5.7.0",
    "@alpha-bytes/generator-sfdxtend": "<%= sfdxtendVersion %>"
  }
}
