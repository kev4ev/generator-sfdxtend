{
  "name": "@alpha-bytes/generator-sfdxtend",
  "version": "0.1.0",
  "description": "Generator to scaffold an sfdxtend extension",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "sfdx",
    "sfdxtend",
    "extend",
    "extension",
    "yeoman-generator"
  ],
  "author": "alpha-bytes",
  "license": "MIT",
  "dependencies": {
    "yeoman-generator": "^5.7.0"
  },
  "devDependencies": {
    "@alpha-bytes/sfdxtend": "~0.1.0"
  }
}
